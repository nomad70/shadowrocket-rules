# Shadowrocket 节点命名规范

本规范用于让 PlayfulSR 配置能根据节点名称，自动识别国家、IP 属性和流量属性。

## 标准格式

```text
CC-NET-CAP-LOCATION-PROVIDER-ID
CC-NET-CAP-LOCATION-PROVIDER-PATH-ID
CC-NET-CAP-LOCATION-PROVIDER-PATH-VARIANT-ID
```

字段之间统一使用半角连字符 `-`，标签统一大写，不在标签内使用空格、中文括号或表情符号。`PATH` 是可选字段，只在需要区分直连、中转或链式出口时添加；`VARIANT` 仅用于区分 `V4`、`V6` 等同一出口变体。

示例：

```text
US-RES-STD-IAD-PROXYSELLER-VIA-BWG-01
US-ISP-STD-SLC-IPRAFT-DIR-01
US-ISP-STD-SLC-IPRAFT-VIA-BWG-01
US-DC-STD-LAX-BWG-RELAY-01
US-DC-STD-LAX-BWG-DIR-V4-01
DE-RES-UL-LIM-RABISU-DIR-01
JP-DC-BIG-TYO-GC-01
SG-DC-BIG-SIN-GC-01
NG-RES-STD-ABV-SUBURBAN-DIR-01
```

## 1. 国家或地区（CC）

| 标签 | 地区 |
| --- | --- |
| `US` | 美国 |
| `JP` | 日本 |
| `SG` | 新加坡 |
| `HK` | 中国香港 |
| `DE` | 德国 |
| `NL` | 荷兰 |
| `TR` | 土耳其 |
| `NG` | 尼日利亚 |

国家标签必须放在节点名称最前面。例如德国节点使用 `DE-`，不要使用 `Germany-DE-` 或将 `DE` 放在名称中间。

## 2. IP 属性（NET）

| 标签 | 含义 | 使用建议 |
| --- | --- | --- |
| `RES` | 住宅级可信出口：真住宅，或经长期验证达到身份敏感用途标准的优质静态 ISP | AI 账号、注册登录、Bybit、地区及风控敏感服务 |
| `ISP` | 表现优于普通机房，但尚未达到或尚未验证为住宅级可信出口 | 日常浏览、Google、流媒体、地区解锁和普通账号登录 |
| `DC` | 普通数据中心、Hosting 或 VPS 出口 | 通用代理、测速、流媒体和大文件下载 |

`RES` 是操作等级，不再宣称节点一定来自物理家庭宽带。对个人配置而言，真正住宅出口通常难以长期、稳定地获得；固定、干净、地区稳定的优质 ISP 往往更适合需要长期身份一致性的服务。因此，经过验证的静态 ISP 可以晋升为 `RES`。

优质 ISP 晋升为 `RES` 前，建议同时满足：IP 长期固定；国家和城市定位稳定；多家数据库整体表现良好；没有明显 VPN、Proxy、Tor 或高风险标记；目标 AI、金融或账号服务长期使用正常；验证码和风控频率较低。若之后出现地区漂移、风险标记增加、共享滥用或登录异常，应降回 `ISP`。

`ISP` 是“增强型日常出口”。像 IPRAFT 这类数据库结论不一致、实际兼容性优于普通 DC，但尚未达到身份敏感用途标准的服务器，继续标记 `ISP`。ProxySeller 的优质 Verizon 静态地址、Rabisu 德国优质 ISP 等经过实际验证后，可以按本地用途标记 `RES`。标签表达的是路由信任等级，不是对 IP 基础设施的公开鉴定。

## 3. 流量使用等级（CAP）

`CAP` 表示“是否允许路由策略将大流量任务分配给该节点”，不是套餐账面流量的客观记录。

| 标签 | 操作定义 | 典型场景 | 配置行为 |
| --- | --- | --- | --- |
| `UL` | 已确认无限或近乎无限，且明确允许超大流量任务 | 50～100GB 以上的 AI 模型、镜像、备份、持续下载 | 进入“无限流量”和“大流量”节点组 |
| `BIG` | 仍有限额，但明确允许承担较大流量 | YouTube/Netflix、数 GB 到数十 GB 下载、`UL` 的备用 | 进入“大流量”节点组 |
| `STD` | 不允许自动承担大流量，与套餐容量大小无关 | 普通上网、AI 账号、Bybit、可信身份出口、精品线路、中转 | 不进入上述两个容量组 |

即使 BWG 或 IPRAFT 有 2TB 流量，只要主要用于中转、优化线路、身份稳定或普通上网，不希望模型下载自动占用，仍应标记 `STD`。只有已明确分配给高流量任务的计量节点才使用 `BIG`。

## 4. 位置、提供方和编号

`LOCATION` 建议使用三字母城市或机场代码，例如：

| 代码 | 城市 |
| --- | --- |
| `LAX` | 洛杉矶 |
| `NYC` | 纽约 |
| `SJC` | 圣何塞 |
| `IAD` | 华盛顿/阿什本 |
| `SLC` | 盐湖城 |
| `FRA` | 法兰克福 |
| `LIM` | 林堡 |
| `TYO` | 东京 |
| `SIN` | 新加坡 |
| `HKG` | 香港 |
| `AMS` | 阿姆斯特丹 |
| `IST` | 伊斯坦布尔 |
| `LAG` | 拉各斯 |
| `ABV` | 阿布贾 |
| `BTZ` | 布尔萨 |

`PROVIDER` 使用简短且稳定的提供方名称，例如 `BWG`、`IPRAFT`、`RABISU`。`ID` 使用两位数字，例如 `01`、`02`。若同一节点需要区分 IPv4/IPv6，把 `V4` 或 `V6` 放在编号前，例如 `US-DC-STD-LAX-BWG-DIR-V6-01`，不要用 `V6` 代替编号。

## 5. 节点角色与路径（PATH，可选）

| 标签 | 含义 | 例子 |
| --- | --- | --- |
| `DIR` | 直接连接的最终出口 | `US-ISP-STD-SLC-IPRAFT-DIR-01` |
| `VIA-BWG` | 最终出口通过 BWG 中转 | `US-ISP-STD-SLC-IPRAFT-VIA-BWG-01` |
| `RELAY` | 仅用作中间跳，不是业务需要的最终出口 | `US-DC-STD-LAX-BWG-RELAY-01` |
| `REALM` | 使用 Realm 转发，仅在需要区分中转实现时添加 | `US-ISP-STD-SLC-IPRAFT-VIA-BWG-REALM-01` |

`RELAY` 是 Relay（中转），不是 Reality。不再使用容易与 Reality 混淆的 `RLY` 缩写。

V2.1 会识别名称中的完整字段 `-RELAY-`，并将该节点自动排除出默认测速、国家出口、美国可信 IP、无限流量和大流量组。节点本身仍保留在 Shadowrocket 中，可继续作为链式代理的中间跳。

默认情况下，节点就是可选的最终出口，因此不必额外写 `EXIT`。Shadowrocket 已在节点名称下方显示 VLESS、TLS、Reality 等协议信息，通常不必在节点名中重复 `TLS` 或 `REALITY`。

如果同一出口存在多种必须手动区分的接入方式，可再增加简短后缀：

```text
US-DC-STD-LAX-BWG-RELAY-TLS-01
US-ISP-STD-SLC-IPRAFT-DIR-REALITY-01
```

这些协议后缀只用于人工识别，不参与当前分流。

## 6. 最终出口原则

链式节点的国家、IP 属性和流量等级必须描述“网站最终看到的出口”，而不是中间跳。

```text
中国设备 → 美国 BWG 中转 → 美国 IPRAFT 双 ISP 出口
```

应命名为：

```text
US-ISP-STD-SLC-IPRAFT-VIA-BWG-01
```

只有单独存在、专门作为第一跳的 BWG 节点才命名为：

```text
US-DC-STD-LAX-BWG-RELAY-01
```

## 7. 配置中的自动识别

| 节点名 | 国家组 | 美国可信组 | 无限流量组 | 大流量组 |
| --- | --- | --- | --- | --- |
| `US-RES-STD-IAD-PROXYSELLER-VIA-BWG-01` | 美国 | 是 | 否 | 否 |
| `US-ISP-STD-SLC-IPRAFT-DIR-01` | 美国 | 否 | 否 | 否 |
| `DE-RES-UL-LIM-RABISU-DIR-01` | 德国 | 否 | 是 | 是 |
| `NG-RES-STD-ABV-SUBURBAN-DIR-01` | 尼日利亚 | 否 | 否 | 否 |

Shadowrocket 实际是用正则表达式读取节点名称，不会自行检测节点是否达到可信身份等级或是否允许无限流量。标签是本地管理元数据，必须按实际验证结果维护。

`US-ISP-STD-*` 会正常进入美国节点和默认测速组，可用于普通上网，但不会进入大流量下载池。所有国家的 `RES` 都不进入全局自动测速；`US-RES-*` 也不进入美国通用组，只由“美国可信IP”组管理。`RES-UL` 仍会进入无限流量和大流量组，因为 `CAP` 是独立的使用授权。

## 8. 订阅节点的注意事项

- 订阅更新可能恢复节点提供方的原始名称。
- 如果手动改名不能持久保留，应在自己控制的订阅生成或本地转换步骤中加标签。
- 不要将包含 token 的订阅 URL 提交到 GitHub，也不要把订阅链接交给不可信的公共转换服务。
- 完成改名后，刷新 Shadowrocket 订阅与策略组，然后检查国家组、美国可信组和容量组的成员是否正确。

## 9. 新节点命名检查清单

1. 国家代码是否在最前面。
2. `RES` 是否已通过固定性、地区稳定性、数据库表现和实际业务验证。
3. `UL/BIG` 是否真的允许路由策略分配大流量任务。
4. 标签是否全部大写且使用半角 `-` 分隔。
5. 链式节点的前三个字段是否描述最终出口。
6. 只做中间跳的节点是否标记 `RELAY`。
7. 节点是否出现在预期的 Shadowrocket 策略组中。
